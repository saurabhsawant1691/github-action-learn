name: Learn basic of GitHub Action

on:
  push:
    branches:
      -  main

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: print hellow
        run: echo "hello world !!"
      - name: print challo
        run: echo "hello challo !!"

  challo-world:
      runs-on: ubuntu-latest
  
      steps:
        - name: print hellow
          run: echo "hello world !!"
        - name: print challo
          run: echo "hello challo !!"
